export class createDeviceModel {
  userId: string;
  ip: string;
  title: string;
  deviceId: string;
  lastActiveDate: string;
}
export class deviceViewModel {
  ip: string;
  title: string;
  lastActiveDate: string;
  deviceId: string;
}
